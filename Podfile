# Uncomment the next line to define a global platform for your project
 platform :ios, '10.1'

target 'MySimpleNoteTaking' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MySimpleNoteTaking
    pod 'RealmSwift'

  target 'MySimpleNoteTakingTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
    pod 'KIF'
  end

  target 'MySimpleNoteTakingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
